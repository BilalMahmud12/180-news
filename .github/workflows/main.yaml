on: push
name: Publish Website
jobs:
  web-deploy:
    name: Website Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.1.0
        with:
          server: ftp://188.40.30.18/
          username: ssstvc_0
          password: ${{ secrets.FTP_PASS }}